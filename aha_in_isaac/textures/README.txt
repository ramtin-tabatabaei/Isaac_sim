Drop object texture images here (.png/.jpg).
Reference them from object_appearance_config.json, e.g. "texture": "textures/basketball.png".
Note: the exported meshes have no UV coordinates, so a texture samples ~one color;
the 'color' field is used as the fallback/tint.
