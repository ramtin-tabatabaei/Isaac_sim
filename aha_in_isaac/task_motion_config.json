{
  "_README": [
    "Per-task arm-motion configuration for run_scene.py.",
    "WHERE the arm goes comes entirely from each task's scene-context .md (the",
    "waypoints); the robot simply follows those waypoint positions in order.",
    "This file ONLY sets how many sim steps the arm spends reaching each named",
    "waypoint - more steps = slower, smoother motion. A waypoint not listed here",
    "uses a built-in default. Tasks fall back to '_default' for anything unset.",
    "  waypoint_steps : { waypoint-name: sim steps to spend reaching it }",
    "  gripper_closed : per-finger close target (m). Lower = closes more (smaller",
    "                   objects); raise if it crushes/shoves. Fully open is fixed",
    "                   at 0.04 (the Franka finger limit), so only this is tunable.",
    "  planner : default planner when --planner is omitted (diffik/rmpflow/curobo/rrt).",
    "  release_at_waypoint : name of the waypoint where the gripper OPENS (releases the",
    "                   object). Default = the last waypoint. Set it earlier to place the",
    "                   object and then retreat with the gripper already open (e.g. open on",
    "                   the scale tray at 'waypoint4', then move up to 'waypoint5').",
    "  pick_place_sequence : true for MULTI-pick tasks (N objects -> a stack/pyramid). The gripper",
    "                   CLOSES at each waypoint whose parent is a graspable (dynamic) body and OPENS",
    "                   at each waypoint whose parent is a placement target, so every object is",
    "                   picked and placed in turn - instead of grasping one object and dragging it",
    "                   through the rest of the trajectory (which rams the other objects -> unstable).",
    "  carry_clearance_m : (pick_place_sequence only) extra height (m) added to the transit/lift",
    "                   waypoints WHILE carrying, so the held object clears the other objects and the",
    "                   growing stack instead of grazing them (grazing jams the arm -> diffIK blow-up).",
    "  Joint physics is not configured here. It comes from task_data/physics/<task>.json."
  ],
  "_default": {
    "waypoint_steps": {},
    "gripper_closed": 0.01,
    "planner": "diffik"
  },
  "close_box": {
    "follow_path_orientation": true
  },
  "block_pyramid": {
    "pick_place_sequence": true,
    "carry_clearance_m": 0.08
  },
  "wipe_desk": {
    "waypoint_steps": {
      "waypoint0": 250,
      "waypoint1": 220,
      "waypoint2": 260,
      "waypoint3": 750,
      "waypoint4": 200
    }
  },
  "basketball_in_hoop": {
    "waypoint_steps": {
      "waypoint0": 200,
      "waypoint1": 180,
      "waypoint2": 320,
      "waypoint3": 260
    },
    "gripper_closed": 0.01
  },
  "beat_the_buzz": {
    "waypoint_steps": {},
    "planner": "rrt"
  },
  "weighing_scales": {
    "waypoint_steps": {},
    "gripper_closed": 0.0,
    "release_at_waypoint": "waypoint4"
  },
  "change_channel": {
    "waypoint_steps": {},
    "gripper_closed": 0.015,
    "grasp_z_offset_m": 0.008,
    "_comment": "Grasp+carry+place the remote via wp0..wp5 (release at wp5), THEN press the button in a live phase: the button rides the relocated remote, so its recorded wp6/wp7 are stale. press_button reads the button's ACTUAL pose at runtime and presses straight down onto it.",
    "gripper_per_waypoint": {
      "waypoint0": "open",
      "waypoint1": "closed",
      "waypoint2": "closed",
      "waypoint3": "closed",
      "waypoint4": "open",
      "waypoint5": "open"
    },
    "press_button": {
      "object": "target_button_topPlate1",
      "press_waypoint": "waypoint7",
      "approach_height_m": 0.07,
      "press_gap_m": 0.01,
      "steps": 200
    }
  }
}
